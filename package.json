{
  "name": "stockvault-protocol",
  "version": "1.0.0",
  "description": "DeFi lending protocol with synthetic stocks and put options on Mantle",
  "main": "index.js",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:testnet": "hardhat run scripts/deploy.js --network mantleTestnet",
    "demo:oracle": "hardhat run scripts/demo-oracle.js",
    "demo:chainlink": "hardhat run scripts/demo-real-chainlink.js",
    "node": "hardhat node"
  },
  "dependencies": {
    "@chainlink/contracts": "^0.8.0",
    "@openzeppelin/contracts": "^4.9.0",
    "ethers": "^6.14.0"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.22.0",
    "chai": "^4.3.0",
    "mocha": "^10.0.0",
    "dotenv": "^16.0.0"
  }
} 